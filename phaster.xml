<tool id="phaster" name="Phaster" version="0.1">
    <description>Searches Phages on PHASTER</description>
    <requirements>
        <!-- Should just use the ncbi tools that's installed in the path -->
        <requirement type="binary">wget</requirement>
    </requirements>
    <command><![CDATA[
        sed '/^>/d' $genome > input.txt ;  tr -d '\n' < input.txt > output.txt ; sed -i.bak 's/^/>fasta_seq/' output.txt; gsed -i.bak 's/>fasta_seq/&\n/' output.txt;
        &&
        bash retrieve_phaster.sh;
      
    ]]></command>

    <inputs>
        <param name="genome" type="fasta" label="Reference Genome (fasta)"/>
    </inputs>
    <outputs>
        
        <data name="output" type="fasta" from_work_dir="output1.fasta">
            </data>
    </outputs>
    
    
    <help>
        **What it does**
        
        Retrieves phages from PHASTER
        
        
        
    </help>
</tool>